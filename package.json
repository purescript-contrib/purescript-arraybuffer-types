{
  "private": true,
  "scripts": {
    "clean": "rimraf output",
    "build": "psa --censor-lib --strict 'src/**/*.purs'"
  },
  "devDependencies": {
    "purescript-psa": "^0.6.0",
    "purescript": "^0.12.0",
    "rimraf": "^2.6.1"
  }
}
