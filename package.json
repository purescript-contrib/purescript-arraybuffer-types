{
  "private": true,
  "scripts": {
    "clean": "rimraf output",
    "build": "psa --censor-lib --strict 'src/**/*.purs'"
  },
  "devDependencies": {
    "purescript-psa": "^0.7.3",
    "rimraf": "^3.0.2"
  }
}
